:schedule:
  # ScAddWorker:
  #   cron: '0 * * * * *'   # Runs once per minute
  #   class: ScAddWorker
  #   args: ['./cotizaC.mdb']
  hello_world2:
    every: '10s'
    #cron: '0 */1 * * * *'
    class: HelloWorld
